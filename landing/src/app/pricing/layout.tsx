export const metadata = {
  title: "Pricing - mycel",
  description:
    "Simple, transparent pricing. Run mycel locally for free, or scale to the cloud when you're ready.",
};

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
