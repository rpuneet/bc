# Handoff: Hook-Driven Agent State

## Completed Work

### 1. Removed Polling/Reconciler System (~690 lines deleted)

| Removed | File | What it did |
|---------|------|-------------|
| `RunReconciler()` | `pkg/agent/agent.go` | 10s background loop calling RefreshState |
| `RefreshState()` | `pkg/agent/agent.go` | Listed tmux/docker sessions, captured pane output, inferred state |
| `detectAgentState()` | `pkg/agent/agent.go` | Symbol-based tmux screen-scraping |
| `captureLiveTask()` | `pkg/agent/agent.go` | Tmux pane capture for activity summaries |
| `StatsCollector` + `Run()` | `pkg/agent/stats.go` | 30s Docker stats polling goroutine |
| `collectDockerStats()` | `pkg/agent/stats.go` | `docker stats --no-stream` shell-out |
| `fetchDockerStats()` | `pkg/agent/stats.go` | Docker JSON output parsing |
| `parseDockerPct/Memory/IO/Bytes` | `pkg/agent/stats.go` | Docker human-readable byte parsing |
| `saveAgentStats()` | `pkg/agent/agent.go` | Stats persistence (write path) |
| `AgentService.Refresh()` | `pkg/agent/service.go` | Wrapper around RefreshState |
| `BuildSetupCommand()` | `pkg/agent/role_setup.go` | Deprecated no-op with zero callers |

**Goroutines removed from `cmd/bcd/main.go`:**
```go
// DELETED:
go agentMgr.RunReconciler(ctx, 10*time.Second)
statsCollector := bcagent.NewStatsCollector(agentMgr)
go statsCollector.Run(ctx)
```

### 2. Rewrote hooks.go — File-Based to HTTP

`pkg/agent/hooks.go` completely rewritten:

**Before:** 3 file-based events (`pre_tool_use`, `post_tool_use`, `stop`) writing to `.bc/agents/{name}/hook_event`

**After:** 22 Claude Code hooks + 4 bc-internal events, all POSTing rich JSON to `POST /api/agents/{name}/hook`

```
SessionStart, SessionEnd, UserPromptSubmit, PreToolUse, PostToolUse,
PostToolUseFailure, PermissionRequest, Stop, StopFailure, Notification,
SubagentStart, SubagentStop, TaskCompleted, TeammateIdle, InstructionsLoaded,
ConfigChange, WorktreeCreate, WorktreeRemove, PreCompact, PostCompact,
Elicitation, ElicitationResult
```

bc-internal events: `ChannelMessage, ChannelSent, AgentMessage, CostUpdate`

New types added:
- `HookPayload` struct with rich metadata (tool_name, command, subagent_type, error, cost fields)
- `IsKnownEvent()` for informational events
- `WriteWorkspaceHookSettings()` generates HTTP curl hooks for all 22 events
- Tool-aware hooks read stdin JSON via jq (PreToolUse, PostToolUse, SubagentStart)

### 3. Server Handler Updated

`server/handlers/agents.go` `/hook` endpoint:
- Accepts `HookPayload` JSON
- Maps event to state via `StateForHookEvent()`
- Supports explicit state override from payload
- Publishes `agent.hook` SSE event via `hub.Publish()` with full metadata
- `hub` field added to `AgentHandler` (passed from `server.go`)

### 4. Stale Code Cleanup

- Removed orphaned `writeRoleCLAUDEMD` comment fragment
- Updated all "reconciler" references in comments
- Removed `RefreshState` references across container.go, stats.go, stats_test.go
- Removed all tests for deleted functions
- Updated `hooks_test.go` to verify 22 HTTP hooks

### 5. Settings.json Fix

Removed `StopFailure` from `.claude/settings.json` — not a valid Claude Code hook event name. Caused Claude Code to reject the entire settings file.

### 6. PR Created

Branch: `worktree-bc-bc-root` — PR #2606 (also #2605, closed as superseded)

### 7. Build and Deploy Verified

- `make clean build` — full build succeeds (go + web + tui + landing)
- `./bin/bcd --addr localhost:9374 --workspace ~/Projects/bc` — starts clean
- All endpoints respond: `/health`, `/api/agents` (10 agents loaded), `/api/agents/{name}/hook`
- Playwright verified: Dashboard + Agents page render correctly
- `go test -race ./pkg/agent/ ./pkg/stats/` — all pass

---

## CRITICAL Open Issues

### 1. Docker Agents Can't Reach bcd

**Problem:** `WriteWorkspaceHookSettings()` in `hooks.go:191` hardcodes:
```go
bcdAddr := "http://127.0.0.1:9374"
```

Docker agents POST hooks to `127.0.0.1:9374` which is localhost *inside the container*. Hooks fail silently (`|| true`).

**Impact:** Docker agents run but state never updates in bcd or web UI.

**Fix:** Add runtime parameter to `WriteWorkspaceHookSettings()`:
- tmux: `http://127.0.0.1:9374`
- Docker: `http://host.docker.internal:9374`

The Python script (`scripts/configure-hooks.py`) already handles this. The Go function needs the same.

### 2. `StopFailure` Still Generated by Go Code

`WriteWorkspaceHookSettings()` at `hooks.go` still generates a `StopFailure` hook entry. Claude Code rejects this key. It was manually removed from `.claude/settings.json` but will be re-added next time the function runs.

**Fix:** Remove the `"StopFailure"` line from the hooks map in `WriteWorkspaceHookSettings()`. Keep `HookStopFailure` in `hookEventStateMap` for the HTTP endpoint.

### 3. Root Agent Stuck After Startup

Likely causes:
1. If Docker: hooks can't reach bcd (issue #1)
2. If tmux: check `MYCEL_AGENT_ID` env var is set, check `.claude/settings.json` exists in worktree
3. Hook scripts may not be configured in the agent's worktree

### 4. sharp-wolf Working But No Frontend Updates

Same root cause as #1 — Docker agent hooks can't POST to bcd.

---

## Technical Debt (Lower Priority)

- **File-based hook code is orphaned** — `ConsumeHookEvent()`, `hookEventFile`, `hookEventPath` have no production callers. Can be removed.
- **`SaveStats` write path is dead** — no production caller since StatsCollector was removed. Read path (`GET /api/agents/{name}/stats`) returns empty.
- **`ListSessions` has no production callers** — was only called by RefreshState. Still in `runtime.Backend` interface.
- **`DetectState` on Provider interface is dead** — used by removed `detectAgentState()`. All 6 providers implement it but it's never called.
- **`HookedWork` field on Agent struct** — stored in SQLite, never set or read in production.
- **Duplicate route** — `/api/workspace/roles` vs `/api/roles` serve same data.

---

## Files Modified (in PR)

```
cmd/bcd/main.go             — Removed RunReconciler + StatsCollector goroutines
pkg/agent/agent.go          — Removed 5 functions (~220 lines), fixed comments
pkg/agent/agent_test.go     — Removed tests for deleted functions (~146 lines)
pkg/agent/hooks.go          — REWRITTEN: 3 file hooks -> 22 HTTP hooks + HookPayload
pkg/agent/hooks_test.go     — Updated to verify HTTP-based hooks
pkg/agent/role_setup.go     — Removed dead BuildSetupCommand
pkg/agent/runtime_test.go   — Removed RefreshState_MixedBackends test
pkg/agent/service.go        — Removed Refresh(), fixed comment
pkg/agent/stats.go          — Gutted: removed StatsCollector + Docker parsing (~197 lines)
pkg/agent/stats_test.go     — Removed Docker parsing tests
pkg/container/container.go  — Removed stale RefreshState comment
pkg/stats/stats.go          — Removed RefreshState call + unused import
pkg/stats/stats_test.go     — Fixed stale comment
server/handlers/agents.go   — Updated comment
.claude/settings.json       — Removed invalid StopFailure hook key
```

---

## Build and Deploy

```bash
make clean build
./bin/bcd --addr localhost:9374 --workspace ~/Projects/bc &
curl http://localhost:9374/health
go test -race ./pkg/agent/ ./pkg/stats/
```

---

## Next Steps (Priority Order)

1. **Fix Docker hook address** — `hooks.go:191` needs runtime-aware bcd address
2. **Remove `StopFailure` from hook generation** — invalid Claude Code hook key
3. **Debug root agent stuck** — check runtime, hook config, env vars
4. **Remove dead file-based hook code** — `ConsumeHookEvent` and friends
5. **Web UI: display hook activity** — add `agent.hook` to WSEventType, show in AgentDetail
6. **Remove dead `ListSessions`/`DetectState`** from interfaces

## Session Note

Working directory (`~/.claude/worktrees/bc-bc-root`) was deleted mid-session by agent cleanup, ending shell access. New session needed from `~/Projects/bc`.
